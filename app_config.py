"""
app_config.py — 앱 전역 설정값 모음

UI 문구, 지원 여행지, 기본 조건 등 앱 전체에서 참조하는 상수를 한곳에 모아 둡니다.
값을 변경하면 UI 텍스트와 비즈니스 로직이 함께 반영됩니다.

설계 원칙:
    - 하드코딩된 문자열이 코드 곳곳에 흩어지는 것을 방지합니다.
    - 향후 다국어 지원이나 여행지 확장 시 이 파일만 수정하면 됩니다.
"""

# ──────────────────────────────────────────────────────────────
# 여행지 스코프 — 현재 앱이 지원하는 유일한 여행지
# ──────────────────────────────────────────────────────────────
SUPPORTED_DESTINATION_NAME = "제주"

# ──────────────────────────────────────────────────────────────
# UI 텍스트 — 헤더·패널 제목·힌트 등
# ──────────────────────────────────────────────────────────────
APP_TITLE = "🏝️ 제주 여행 플래너"      # 상단 헤더에 표시되는 앱 제목
APP_SCOPE_LABEL = "제주 전용"           # 헤더 오른쪽 칩(Chip)에 표시되는 범위 라벨

AGENT_TITLE = "제주 여행 도우미"         # 오른쪽 에이전트 패널 상단 제목
AGENT_SUBTITLE = "제주 여행 조건과 원하는 일정을 입력하세요."  # 에이전트 패널 부제목
AGENT_CONDITION_TITLE = "현재 여행 조건"  # 조건 칩 영역 소제목

# 사용자 입력창(TextField)에 표시되는 힌트 텍스트.
# 줄이 길어 괄호로 묶어 이어 씁니다.
TRAVEL_INPUT_HINT_TEXT = (
    "예: 4박 5일 부모님과 렌터카로, 걷는 일정은 짧게 하고 "
    "바다 보이는 조용한 숙소 위주로 짜줘"
)

# ──────────────────────────────────────────────────────────────
# 가드레일 차단 메시지
# ──────────────────────────────────────────────────────────────
# 사용자가 제주 이외의 여행지를 요청했을 때 에이전트가 표시하는 안내 문구.
JEJU_SCOPE_BLOCK_MESSAGE = (
    "현재 버전은 제주 여행만 지원합니다. "
    "제주 여행 조건으로 다시 입력해 주세요."
)

# ──────────────────────────────────────────────────────────────
# 기본 여행 조건(Default Travel Condition)
# ──────────────────────────────────────────────────────────────
# UI가 최초 렌더링될 때 조건 칩 영역에 기본으로 표시할 조건입니다.
# TravelCondition TypedDict 형태와 동일한 구조를 딕셔너리로 정의합니다.
#
# 필드 설명:
#   category   — 조건 분류. 여기서는 "destination"(여행지).
#   label      — UI 칩에 표시할 짧은 문자열.
#   raw_text   — 사용자 원문 근거. 기본값이므로 "제주"로 채움.
#   confidence — LLM 신뢰도. 기본 조건이므로 1.0(확정).
DEFAULT_TRAVEL_CONDITION = {
    "category": "destination",
    "label": "제주 여행",
    "raw_text": "제주",
    "confidence": 1.0,
}
